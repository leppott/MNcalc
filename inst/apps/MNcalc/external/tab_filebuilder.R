# File Builder Panel

function() {
  tabPanel("tabpan_filebuilder"
           , h2("File Builder")
			, p("Tools to generate (build) a file for calculation.")
           )##tabPanel ~ END
}##FUNCTION ~ END
